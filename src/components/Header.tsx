import React from 'react';

const Header: React.FC = () => {
   return (
      <div className="w-full h-20 text-center bg-red-700">
         Make your Hamburger
      </div>
   );
};

export default Header;
